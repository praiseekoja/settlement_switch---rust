# Example environment variables for Stylus deployment
# Copy this file to `.env` locally (do NOT commit `.env`) and fill in the values.

# RPC URL for the Arbitrum Stylus network you want to deploy to
# e.g. https://sepolia-rollup.arbitrum.io/rpc
ARBITRUM_SEPOLIA_RPC_URL="https://sepolia-rollup.arbitrum.io/rpc"

# Your deployer private key (KEEP THIS PRIVATE). Use a hardware wallet or CI secrets.
# PRIVATE_KEY must NOT be committed to git. In CI, set this as a repository secret.
PRIVATE_KEY="<your-private-key-here>"

# Example optional override for cargo stylus (if needed)
# CARGO_UNSTABLE_BUILD_STD="core,alloc"
