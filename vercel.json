{
  "buildCommand": "pnpm --prefix frontend install && pnpm --prefix frontend run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "echo 'Installing dependencies'",
  "framework": "nextjs"
}

